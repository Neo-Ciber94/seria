{
    "root": true,
    "name": "seria-monorepo",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "test": "turbo run test",
        "build": "turbo run build --filter ./packages/**/*",
        "lint": "turbo run lint"
    },
    "devDependencies": {
        "turbo": "^1.13.0"
    }
}